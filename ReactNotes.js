/*
EP#3

- npm scripts
- React Elements are the plain objects that are similar to the DOM elements, when we render react elements then it becomes HTML.
- Plain Objects mean Object that have one or more key-value pair

- JSX in not HTML its syntax looks like a HTML.
- jSX code is converted into the React.createElement.
- Babel is converting JSX to React.createElement.

- React Components
- Class Based Components
- Functional Components


EP#4
Config driven UI:- This is the UI that is made according to data, suppose if we want to display speical offers in Lahore and spearte offers in different cities we have to make this config driven UI. 



*/